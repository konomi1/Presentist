<% if current_user.following?(user) %>
  <%= link_to "フォロー解除", relationships_path(user.id), method: :delete %>
<% else %>
  <%= link_to "フォローする", relationships_path(user.id), method: :post %>
<% end %>