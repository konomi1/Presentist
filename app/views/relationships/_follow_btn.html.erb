<% if current_user.following?(user) %>
  <%= link_to "フォロー中", relationships_path(user.id), method: :delete, remote: true, class: "unfollow-btn" %>
<% else %>
  <%= link_to "フォローする", relationships_path(user.id), method: :post, remote: true, class: "following-btn" %>
<% end %>