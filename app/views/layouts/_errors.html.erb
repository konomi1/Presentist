<% if obj.errors.any? %>
  <div id="error_unit">
    <h2><%= obj.errors.count %>件のエラーが発生しました。ご確認お願いいたします。</h2>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
