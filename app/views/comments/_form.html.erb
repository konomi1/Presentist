<%= form_with model:[present, comment] do |f| %>
  <%= f.text_area :comment, autofocus: true, placeholder: "贈り物ログへのコメント", class: "form-control" %><br />
  <%= f.submit "コメントする", class: "btn btn-primary" %>
<% end %>