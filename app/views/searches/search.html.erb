<h1 class="title">Resolut</h1>
<p class="sub-title"></p>

<div class="search-wrap">
  <h3>"<%= @content %>"の検索結果</h3>
  <% if @results.exists? %>
    <%= render 'presents/index', presents: @results %>
  <% else %>
    <p>該当するデータがありません。</p>
  <% end %>
</div>